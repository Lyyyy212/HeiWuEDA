# Vendored parser runtime

Only these files from TypeScript 5.9.3 are used by the indexer:

- `package/lib/typescript.js`
- `package/package.json`
- `package/LICENSE.txt`
- `package/ThirdPartyNoticeText.txt`

They were extracted from `../typescript-5.9.3.tgz`. The archive integrity is pinned in the parent README and matches the TypeScript package integrity already locked by the official EasyEDA `pro-api-sdk` snapshot.
